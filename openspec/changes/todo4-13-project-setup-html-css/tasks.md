## 1. Create index.html with HTML5 skeleton

- [x] 1.1 Create index.html with DOCTYPE, html lang="en", head (charset, viewport, title "Todo App"), and empty body
- [x] 1.2 Add div#app container in body with semantic sections: header (h1 "Todo App"), main, footer

## 2. CSS Reset and Custom Properties

- [x] 2.1 Add inline <style> tag with CSS reset: box-sizing border-box on *, *::before, *::after; body margin/padding 0; system font stack; antialiased rendering
- [x] 2.2 Define all 13 CSS custom properties on :root (colors, spacing, radius, shadow)

## 3. Base Styles and Layout

- [x] 3.1 Apply base body styles using custom properties: background var(--color-bg), color var(--color-text), min-height 100vh
- [x] 3.2 Style #app container: max-width 600px, auto margins, padding, background var(--color-surface), box-shadow var(--shadow), border-radius var(--radius)
- [x] 3.3 Style header, main, and footer sections with appropriate spacing

## 4. Validation

- [x] 4.1 Verify page renders correctly: centered container, heading visible, no console errors, no horizontal scroll below 600px
